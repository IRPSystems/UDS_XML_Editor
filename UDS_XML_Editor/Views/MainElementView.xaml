<UserControl x:Class="UDS_XML_Editor.Views.MainElementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:UDS_XML_Editor.Models"
             xmlns:local="clr-namespace:UDS_XML_Editor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <TextBlock Text="Bla" />
        <!--<TreeView ItemsSource="{Binding}">

            <TreeView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </TreeView.ItemTemplate>

            <TreeView.Resources>
                --><!--<HierarchicalDataTemplate DataType="models:Customer" ItemsSource="{Binding FWStepsList}">
                    
                </HierarchicalDataTemplate>--><!--
                
                
                
            </TreeView.Resources>

        </TreeView>-->
        
    </Grid>
</UserControl>
